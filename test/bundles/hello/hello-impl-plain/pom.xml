<?xml version='1.0' encoding='UTF-8'?>
<!--
  ~ Copyright (c) jGuru Europe AB.
  ~ All rights reserved.
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
    ################################################
    # Section 1:  Project information
    ################################################
    -->
    <parent>
        <groupId>se.jguru.nazgul.core.test.bundles.parent</groupId>
        <artifactId>nazgul-core-test-bundles-parent</artifactId>
        <version>1.0.1-SNAPSHOT</version>
        <relativePath>../../parent</relativePath>
    </parent>

    <groupId>se.jguru.nazgul.core.test.bundles.hello.impl.plain</groupId>
    <artifactId>nazgul-core-test-hello-impl-plain</artifactId>
    <name>${project.groupId}.${project.artifactId}</name>
    <packaging>bundle</packaging>

    <!--
    ################################################
    # Section 2:  Dependency (management) settings
    ################################################
    -->
    <dependencies>
        <!--
            Internal dependencies
        -->
        <dependency>
            <groupId>se.jguru.nazgul.core.test.bundles.hello.api</groupId>
            <artifactId>nazgul-core-test-hello-api</artifactId>
            <version>${test.bundles.version}</version>
        </dependency>

        <!--
            External dependencies.
        -->
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.compendium</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
    </dependencies>

    <!--
    ################################################
    # Section 3:  Build settings
    ################################################
    -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Bundle-Activator>se.jguru.nazgul.core.test.bundles.hello.impl.plain.PlainHelloActivator</Bundle-Activator>
                    </instructions>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!--
       Define connections for Maven's Mercurial integration.
    -->
    <scm>
        <developerConnection>scm:hg:file://${basedir}/../../../../</developerConnection>
        <tag>HEAD</tag>
    </scm>
</project>